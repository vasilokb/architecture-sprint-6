apiVersion: v1
kind: Service
metadata:
  name: scaletestapp-service
  labels:
    prometheus-monitored: "true"  # Добавляем для ServiceMonitor
spec:
  selector:
    app: scaletestapp
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: LoadBalancer